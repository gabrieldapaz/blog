<h1>INDEX PAGE!!</h1>

<p> <% blogs.forEach(function(blog){ %></p>
    <div>
        <h2> <%= blog.title%></h2>
        <img src="<%=blog.image%>" alt="">
        <span><%= blog.created%></span>
        <p> <%= blog.body%></p>
    </div>

<% }) %>